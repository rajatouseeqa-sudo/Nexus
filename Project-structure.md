src folder

- components → choti choti UI cheezen (buttons, cards)
- pages → full screens (login, dashboard)
- assets → images
- hooks → reusable logic
- context → global data (user info etc.)
- services → API calls (backend se data lena)
- App.jsx → main app file
- main.jsx → app start hota hai yahan

Architecture

- React app use ho rahi hai
- Routing pages ke through hoti hai
- Components reusable banaye gaye hain
- Data API ya context se manage hota hai
